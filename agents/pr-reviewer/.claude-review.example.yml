# Copy to .claude-review.yml in the repository root and adjust per project.

disabled_rules:
  # - TODO_IN_CHANGE

ignored_paths:
  - docs/**
  - examples/**
  - generated/**

severity_overrides:
  DEPENDENCY_CHANGE: Low
  CHANGED_SYMBOL_WITHOUT_RELEVANT_TEST: Medium
